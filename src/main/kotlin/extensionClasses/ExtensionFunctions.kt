package extensionClasses

class Utils {

    fun upperCaseFirstAndLast(string : String) : String {
        val upperFirst = string.substring(0,1).toUpperCase() + string;
        return "something"
    }
}